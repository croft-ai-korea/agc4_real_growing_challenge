apiVersion: 1

datasources:
  - name: TimescaleDB
    type: postgres
    access: proxy
    url: timescaledb:5432
    user: postgres
    password: admin1234
    database: postgres
    isDefault: true
    jsonData:
      sslmode: disable